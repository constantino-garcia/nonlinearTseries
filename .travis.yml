# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

env:
    GL_USE_NULL=TRUE

r:
    - 3.1
    - 3.2
    - oldrel
    - release
    - devel
